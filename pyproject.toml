[project]
name = "rubi-gto"
version = "0.1.0"
description = "Pipeline for generating Rubi-compatible Japanese Minecraft lang packs"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
